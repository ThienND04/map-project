const COLOR_DEFAULT = [90, 237, 0]; 
const COLOR_FOUND = [42, 37, 204];    
const COLOR_SELECTED = [255, 83, 47]; 

const Colors = {
    COLOR_DEFAULT,
    COLOR_FOUND,
    COLOR_SELECTED
}

export default Colors;